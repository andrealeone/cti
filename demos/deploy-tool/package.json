{
  "name": "concise-ti-demo-deploy-tool",
  "version": "1.0.0",
  "description": "Deployment CLI with flags and output styling built with concise-ti",
  "keywords": [
    "concise-ti",
    "demo",
    "cli"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun run ./main.ts",
    "build": "bun build ./main.ts --compile --outfile dist/deploy"
  }
}
