{
  "name": "concise-ti-demo-hello-world",
  "version": "1.0.0",
  "description": "Simple greeting CLI built with concise-ti, using command auto-discovery",
  "keywords": [
    "concise-ti",
    "demo",
    "cli"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun run ./main.ts",
    "cli": "bun run ./main.ts",
    "build": "bun ../../bin/cli.ts compile ./main.ts --outfile dist/hello"
  }
}
